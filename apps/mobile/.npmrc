# RN/Metro плохо работает с pnpm symlinked node_modules — некоторые пакеты
# (expo, @babel/runtime) require'aт зависимости через relative resolve и
# не находят их в .pnpm/ структуре. Hoisted даёт плоский node_modules.
node-linker=hoisted
