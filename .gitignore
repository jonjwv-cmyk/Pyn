# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
build/
out/
*.tsbuildinfo

# Build resources — source-картинку логотипа (PNG 1024+ или SVG fallback)
# держим в репо. Генерируемые иконки (.ico / .icns / итоговый icon.png) —
# нет, их собирает scripts/build-icons.mjs в CI.
!apps/desktop/build/
!apps/desktop/build/icon.svg
!apps/desktop/build/icon-source.png
!apps/desktop/build/README.md
apps/desktop/build/icon.ico
apps/desktop/build/icon.icns
apps/desktop/build/icon.png

# Electron
apps/desktop/dist-electron/
apps/desktop/release/

# Expo / React Native
apps/mobile/.expo/
apps/mobile/web-build/
apps/mobile/android/
apps/mobile/ios/

# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
.cursor/
*.swp
*.swo

# Env / secrets
.env
.env.local
.env.*.local

# Credentials / handoffs — НИКОГДА не коммитить (CF/AWS/Google токены, SSH,
# PAT). §2026-05-27: CF token утёк в HANDOFF_NEW_CHAT.md → GitHub Secret-
# Scanning → CF auto-revoke. Эти паттерны не дадут повториться.
*SECRET*
*SECRETS*
*HANDOFF*
*CREDENTIALS*
*.secrets
secrets.*
**/SECRETS.md

# Logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*
.claude/
